# 메서드란 ?
1. 객체의 구성요소로, 객체가 필요로 하는 기능처리를 위해서 사용한다. 
2. 기능 처리란? 
	1) 데이터를 저장처리(매개변수로 받아 필드값에 저장 처리) 
		ex) 사람이 감각기관으로 눈이라는 메서드를 시각정보를 저장
	2) 특정한 데이터를 조건이나 반복 의해서 return 해준다. 
		ex) 자판기에서 돈을 입력하고, 화면에 입력된 돈을 표기, 
		선택한 버튼에 의해서 음료와 나머지돈이 나온다.
	3) 출력처리(현재 가지고 있는 데이터에 대한 화면 표시) 
		ex)현재 있는 데이터 콘솔창으로 출력하는 것을 말한다.
	4) 조건/반복을 통한 프로세스 처리	
		ex) 자판기에 돈을 입력하면, 입력된 금액에 맞는 물건을 리턴할
			수 있게 조건 처리
		
3. 메서드의 형식
	리턴할 데이터 type		메서드명(입력할 매개변수1, 매개변수2....	){
			프로세스 처리..
			return 결과 데이터;
	
	}
	--리턴할 데이터type이 없는 경우는 void를 제외하고 리턴할 데이터 유형,
	객체 type도 선언할 수 있다.
	
	ex) String name = "홍길동";
	String outputProduct(int chButton){
		String prod="";
		if(chButton==1){
			prod="콜라";
		}
		if(chButton==2){
			prod="사이다";
		}
		return prod;
	}
	
	main()
		String retBev = m.outbutProduct(1);
			//위 메서드 매개변수와 process에 의해서, 1일때는
			//"콜라"라는 문자열을 return하기 위해서 prod라는 문자열을 선언.
			//위 프로세스에 의해 마지막 retBev에는 "콜라"를 가지고 있고..
			//String retBev = m.outbutProduct(2);
			//위 프로세스에 의해 마지막 retBev에는 "사이다"를 가지고 있기 위해
			//String prod="";로 초기 변수를 선언하여 사용합니다.
			//지역변수는 초기를 해주고 처리해주는 것이 일반적이다.

	
	1) 	매개변수에 대한 내용 연습
		이름이 같더라도 매개변수 type, 갯수, 갯수와 타입이 2개 이상일 때,
		순서가 다르면....오버로딩
	
	2)	return값에 대한 연습.
		 문자열,숫자, ...
		 
		 # 메서드의 리턴값 처리..
		 
		1.	객체는 생성자와 필드를 통해서 기본 데이터를 저장하고
			또한, 특정 메서드를 통해서 데이터 변경/수정할 수 있다.

		2.	메서드 기능 중에 특정 데이터가 입력되면, 그 결과 데이터를
			여러 조건/반복을 통해서 원하는 결과를 처리할 수 있다.
	
		3.	메서드 return값은 메서드를 통해서 최종 결과값을 가져오는 것을 말한다.

		4.	기본형식..
			리턴데이터type 메서드명(매개변수1, 매개변수2){
				//처리될 프로세스..
					return 실제 리턴할 값;
			}
				ex)
				int plus(int num01, int num02){
					int sum = num01+num02;
					return sum;
				}
				변수를 선언할 때, 데이터 유형을 선언하고, 
				그 유형에 받는 데이터를 할당 하듯이 int num01=25;
				메서드에서도 return할 데이터의 유형을 선언하고, 실제 데이터를 return 처리한다.
				위의 예제와 같이 입력값 정수로 2개의 데이터를 받고, 합산한 값을 return할때,
				리턴할 결과 유형에 맞게 선언한다..
		 
	
	3) process에 대한 연습..
		 여러가지 예제를 통해서 조건 반복문 처리..

	